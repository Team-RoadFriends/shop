<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.RoadShop.shopping.mapper.OrderMapper">
    <insert id="insertOrder" parameterType="com.RoadShop.shopping.dto.OrderDto"
            useGeneratedKeys="true" keyProperty="id">
            INSERT INTO orders (user_id, order_status, total_amount, shipping_address, created_at)
            VALUES (#{user_id}, #{order_status}, #{total_amount}, #{shipping_address}, #{created_at})
    </insert>

    <select id="getOrders" resultType="com.RoadShop.shopping.dto.OrderDto">
        SELECT order_id, order_status, total_amount, shipping_address, created_at
        FROM orders order by user_id=1 limit 10;
    </select>

</mapper>